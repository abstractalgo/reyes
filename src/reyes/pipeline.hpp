#pragma once

#include "core.h"
#include <list>
#include "shape.hpp"
#include "camera.hpp"
#include "image.hpp"

namespace reyes
{
	void render(std::list<ShapeI> scene, camera camera, ImageI* image)
	{
		// bound-split
		// dice
		// shade
		// sample
	}
}